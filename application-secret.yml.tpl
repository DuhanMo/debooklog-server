kakao:
  api-key: ${kakao rest api key}
oauth2:
  kakao:
    client-id: ${oauth2 kakao client id}
    client-secret: ${oauth2 kakao client secret}
    redirect-uri: ${oauth2 kakao redirect uri}
    scope: ${oauth2 kakao scope}
  google:
    client-id: ${oauth2 kakao client id}
    client-secret: ${oauth2 kakao client secret}
    redirect-uri: ${oauth2 kakao redirect uri}
    scope: ${oauth2 kakao scope}
